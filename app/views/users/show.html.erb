<% provide(:title, @user.name) %>
<div>
  <table class="table table-bordered table-condensed user-table"> <!--borderedボーダー付き、condensed行間の狭い。-->
    <tr>
      <td rowspan="2">時間管理表</td>
      <td>指定勤務開始時間</td>
      <td colspan="3" rowspan="2">基本時間</td>
      <td rowspan="2">初日</td>
    </tr>

    <tr>
      <td>指定勤務終了時間</td>
    </tr>

    <tr>
      <td>所属: <%= @user.affiliation.present? ? @user.affiliation : "未所属" %></td>
      <td>名前：<%= @user.name %></td>
      <td>コード</td>
      <td></td>
      <td>出勤日数</td>
      <td>締め</td>
    </tr>
  </table>
</div>